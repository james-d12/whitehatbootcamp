<h1>{{restaurant.name}}</h1>

<section>
    {{#each menus}}
        <article>
            <dl>
                {{#each this.items}}
                    <h2> {{this.length}} </h2>
                    <div>
                        <dt>{{this.name}}</dt>
                        <dd>£{{this.price}}</dd>
                    </div>
                {{/each}}
            </dl>
        </article>
    {{/each}}
</section>